# source this file to:
# setup a virtualenv (if it isn't already)
# activate the virtualenv
# and install dependencies

if [ ! -d "env" ]; then
  virtualenv -p /usr/bin/python2.7 --prompt=\(ttt-sec\) env
fi

source env/bin/activate
pip install -r requirements.txt

