$(document).ready(function(){	form_submitted = false;		$('#JS-required-container').hide().nextAll().show();		if ($('div.game-over').length > 0) {		game_over = true	}	else {		game_over = false	}		if (!game_over) {		$('#main-grid div').click(function() {			if ($(this).hasClass('not-selected') && !form_submitted) {				// set the value of the input to the last character of the clicked cell's ID.				id = $(this).attr('id')				$('#input-selected-cell').val(id.substring(id.length - 1))								// submit the form				form_submitted = true;				$('body').css('cursor', 'wait')				$(this).css('cursor', 'wait')				$('#main-form').submit()			}		});				$('div.not-selected').hover(function() {			if (!form_submitted) {				$(this).addClass('cell-hover')			}		}, function() {			if (!form_submitted) {				$(this).removeClass('cell-hover')			}		});	}});