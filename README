Intructions:
1. pip install this app
2. create and configure a Django project with 'tictactoe' in the INSTALLED_APPS
3. ./manage.py syncdb
4. run ./manage.py populate_nextmove to prepare the database for the AI
