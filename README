toebot.py implements a simple bot that plays tic-tac-toe.

Run toebot.py for a simple interactive version at the CLI.

server.py, when run, creates a simple HTTP server that allows you to play at http://127.0.0.1:8000
